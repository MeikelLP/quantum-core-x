apiVersion: v1
kind: Service
metadata:
  name: {{ include "quantum-core-auth.fullname" . }}
  labels:
    {{- include "quantum-core-auth.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "quantum-core-auth.selectorLabels" . | nindent 4 }}
